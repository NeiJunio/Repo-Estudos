namespace Testes;

class Program.
{
    static void Main()
    {
        var lista = new List<string> { "Nei", "Junio" };

        for (int i = 0; i < lista.Count; i++)
        {
            Console.WriteLine(lista[i]);
        }
    }
}